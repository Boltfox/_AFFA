dir:        
        #Work_dir is the location where the pipeline will working on
        work_dir: '/home1/AFFA'
        #Home_dir is where the results will be stored 
        home_dir: '/home1/HTRU-N/results'
        #Where to stored H5 files
        h5dir: 'Prepfold' 
para:
        #Downsampling factor 
        downsamp: 16
        #Accleration range 
        a_min: 2 #do not change it, unless you know what you are doing 
        a_max: 4 
        #Config file name
        configname: 'HTRUNll3_manager.yaml'
        generate_yaml : y #y for generate new yaml files for accleration searchs 
        t_obs: 4320 #observation time
